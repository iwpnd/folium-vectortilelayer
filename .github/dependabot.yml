version: 2

updates:
    - package-ecosystem: pip
      directory: /
      schedule:
          interval: weekly
      ignore:
          - update-types: ["version-update:semver-patch"]

    - package-ecosystem: github-actions
      directory: /
      schedule:
          interval: weekly
